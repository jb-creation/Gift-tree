import 'package:flutter/material.dart';

const int kPageSize = 10;
const double kMaxDialogWidth = 320;
const double kMaxDialogHeight = 640;
const double kMaxLayoutWidth = 420;

const BoxConstraints kDialogConstraints = BoxConstraints(maxWidth: kMaxDialogWidth, maxHeight: kMaxDialogHeight);
const BoxConstraints kLayoutConstraints = BoxConstraints(maxWidth: kMaxLayoutWidth);
